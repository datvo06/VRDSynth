from datasets import Dataset
from typing import Optional, List, Dict, Union, Tuple


def calculate_distribution_shift_train_val(dataset_train: Dataset, dataset_val: Dataset, label: str) -> Tuple[float, float]:
    pass


def compute_report(dataset: Dataset):
    pass
